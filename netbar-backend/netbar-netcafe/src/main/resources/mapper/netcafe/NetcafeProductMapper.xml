<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ztw.netbar.netcafe.mapper.NetcafeProductMapper">

    <resultMap type="NetcafeProduct" id="NetcafeProductResult">
        <result property="id"            column="id"             />
        <result property="productName"   column="product_name"   />
        <result property="category"      column="category"       />
        <result property="price"         column="price"          />
        <result property="stock"         column="stock"          />
        <result property="warningStock"  column="warning_stock"  />
        <result property="status"        column="status"         />
        <result property="delFlag"       column="del_flag"       />
        <result property="createBy"      column="create_by"      />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"      column="update_by"      />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"        column="remark"         />
    </resultMap>

    <sql id="selectNetcafeProductVo">
        select id, product_name, category, price, stock, warning_stock, status, del_flag,
               create_by, create_time, update_by, update_time, remark
        from netcafe_product
    </sql>

    <select id="selectNetcafeProductById" parameterType="Long" resultMap="NetcafeProductResult">
        <include refid="selectNetcafeProductVo"/>
        where id = #{id} and del_flag = '0'
    </select>

    <select id="selectNetcafeProductList" parameterType="NetcafeProduct" resultMap="NetcafeProductResult">
        <include refid="selectNetcafeProductVo"/>
        <where>
            del_flag = '0'
            <if test="productName != null and productName != ''">
                AND product_name like concat('%', #{productName}, '%')
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        order by id desc
    </select>

    <insert id="insertNetcafeProduct" parameterType="NetcafeProduct" useGeneratedKeys="true" keyProperty="id">
        insert into netcafe_product (
            <if test="productName != null and productName != ''">product_name, </if>
            <if test="category != null and category != ''">category, </if>
            <if test="price != null">price, </if>
            <if test="stock != null">stock, </if>
            <if test="warningStock != null">warning_stock, </if>
            <if test="status != null and status != ''">status, </if>
            <if test="remark != null and remark != ''">remark, </if>
            <if test="createBy != null and createBy != ''">create_by, </if>
            create_time
        )values(
            <if test="productName != null and productName != ''">#{productName}, </if>
            <if test="category != null and category != ''">#{category}, </if>
            <if test="price != null">#{price}, </if>
            <if test="stock != null">#{stock}, </if>
            <if test="warningStock != null">#{warningStock}, </if>
            <if test="status != null and status != ''">#{status}, </if>
            <if test="remark != null and remark != ''">#{remark}, </if>
            <if test="createBy != null and createBy != ''">#{createBy}, </if>
            sysdate()
        )
    </insert>

    <update id="updateNetcafeProduct" parameterType="NetcafeProduct">
        update netcafe_product
        <set>
            <if test="productName != null and productName != ''">product_name = #{productName}, </if>
            <if test="category != null and category != ''">category = #{category}, </if>
            <if test="price != null">price = #{price}, </if>
            <if test="stock != null">stock = #{stock}, </if>
            <if test="warningStock != null">warning_stock = #{warningStock}, </if>
            <if test="status != null and status != ''">status = #{status}, </if>
            <if test="remark != null">remark = #{remark}, </if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy}, </if>
            update_time = sysdate()
        </set>
        where id = #{id}
    </update>

    <update id="deleteNetcafeProductById" parameterType="Long">
        update netcafe_product set del_flag = '1' where id = #{id}
    </update>

    <update id="deleteNetcafeProductByIds" parameterType="Long">
        update netcafe_product set del_flag = '1' where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="deductStock">
        update netcafe_product
        set stock = stock - #{quantity}, update_time = sysdate()
        where id = #{productId} and stock >= #{quantity}
    </update>

</mapper>
